/** Nom affiché de l'application (App Store, en-têtes, autorisations iOS). */
export const APP_NAME = "SOPK Nutrition";

/** Sous-titre court (logo compact, PWA). */
export const APP_TAGLINE = "Au quotidien";

/** Titre SEO / navigateur. */
export const APP_PAGE_TITLE = `${APP_NAME} - Nutrition SOPK au quotidien`;

/** Nom complet PWA (écran d'accueil). */
export const APP_MANIFEST_NAME = `${APP_NAME} - Au quotidien`;
